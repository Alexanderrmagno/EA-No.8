import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Evidencia de Aprendizaje 8',
  webDir: 'www'
};

export default config;
